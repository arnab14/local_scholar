# Core Libraries
streamlit==1.30.0
requests
beautifulsoup4
langchain>=0.1.0,<0.2.0
langchain-community>=0.0.30
ollama>=0.1.8

# Document Loaders
PyPDF2>=3.0.1 # Keep as potential fallback
# unstructured # REMOVED: To resolve dependency conflicts
PyMuPDF # Use PyMuPDF (fitz) for PDF extraction

# Embeddings and Vector Store
sentence-transformers==2.2.2
torch==2.2.0 # Or the version that worked for your Python 3.12
torchvision
torchaudio
huggingface-hub<0.21.0
chromadb>=0.4.20,<0.5.0

# Utilities
python-dotenv

# Conflict Resolution
protobuf<=3.20.1 # KEEP THIS PINNED LOW to avoid original RuntimeError
